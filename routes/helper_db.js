//only for testing

var express = require('express')
var router = express.Router()

const PlaygroundModel = require('./db/PlaygroundModel')

router.post('/fill-playground-internal', (req,res) => {

})

module.exports = router